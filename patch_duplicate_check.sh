sed -i 's/{item.amount && item.category && pastClaims.some(c => c.id !== resubmitClaimId && c.expenses && c.expenses.some((e: any) => e.category === item.category && e.amount === parseFloat(item.amount))) && (/{item.amount \&\& pastClaims.some(c => c.id !== resubmitClaimId \&\& c.expenses \&\& c.expenses.some((e: any) => e.expense_date === moms.find(m => m.id === selectedMomId)?.meeting_date \&\& e.amount === parseFloat(item.amount))) \&\& (/' src/pages/SubmitClaim.tsx
sed -i 's/<span>A previous claim has the exact same category and amount. Ensure this is not a duplicate.<\/span>/<span>A previous claim has the exact same date and amount. Ensure this is not a duplicate.<\/span>/' src/pages/SubmitClaim.tsx
